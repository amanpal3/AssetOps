# Blockchain Development Skill (Member A)

## Role
Lead smart contract engineering for AssetOps.

## Core Responsibilities
- Implement Solidity ^0.8.24 contracts with OpenZeppelin v5.
- Ensure strict checks-effects-interactions and reentrancy protection (`ReentrancyGuard`).
- Maintain precision with basis points (`rateBps / 10,000`) and checked integer arithmetic.
- Enforce append-only announcement storage and idempotency guards (`mapping(bytes32 => bool)`).
- Execute comprehensive tests in `contracts/test/`.
